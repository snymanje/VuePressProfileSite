@tailwind base;
@tailwind components;
@tailwind utilities;

.theme-default-content:not(.custom) {
  max-width: 1280px !important;
}

.theme-container.custom-page-class > .page > .theme-default-content {
  max-width: 100% !important;
  padding: 0 0 !important;
}